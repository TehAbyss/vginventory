This project was bootstrapped with `dotnet new xunit`.

## Available Scripts

In the project directory, you can run:

### `dotnet test`

Launches the test runner.

### `dotnet build`

Builds the component.
